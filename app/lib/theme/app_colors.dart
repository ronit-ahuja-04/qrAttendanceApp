import 'package:flutter/material.dart';

/// Colors pulled directly from the "Tactile Industrial" design system
/// used across the Stitch-exported screens (see tactile_industrial/DESIGN.md).
class AppColors {
  AppColors._();

  static const surface = Color(0xFFFFF8F1);
  static const surfaceDim = Color(0xFFE0D9D0);
  static const surfaceContainerLow = Color(0xFFFAF3E9);
  static const surfaceContainer = Color(0xFFF4EDE3);
  static const surfaceContainerHigh = Color(0xFFEEE7DE);
  static const surfaceContainerHighest = Color(0xFFE9E1D8);

  static const onSurface = Color(0xFF1E1B16);
  static const onSurfaceVariant = Color(0xFF534434);

  static const outline = Color(0xFF857462);
  static const outlineVariant = Color(0xFFD8C3AE);

  static const primary = Color(0xFF855400);
  static const primaryContainer = Color(0xFFF5A11C);
  static const onPrimaryContainer = Color(0xFF633D00);

  static const secondary = Color(0xFF5F5F59);

  static const error = Color(0xFFBA1A1A);

  // The "putty-stone wall" the whole app sits on.
  static const wall = Color(0xFFDCD8CF);

  // The pressed/debossed well color used for input fields.
  static const debossedWell = Color(0xFFCFCBC1);

  static const amberGlow = Color(0x66F5A11C);

  static const errorContainer = Color(0xFFFFDAD6);
  static const onErrorContainer = Color(0xFF93000A);
  static const secondaryContainer = Color(0xFFE1E0D9);
  static const onSecondaryContainer = Color(0xFF63635E);
  static const inverseSurface = Color(0xFF33302A);
  static const inverseOnSurface = Color(0xFFF7F0E6);
}
